content를 함수로 바꾼다
push 
push() 메서드는 배열의 끝에 하나 이상의 요소를 추가하고, 배열의 새로운 길이를 반환합니다.
-> return 값이 number
왜 지연?
document.querySelector니까
document-> html -> element -> DOM(document object model)
defer를 써서 강제로 늦춰 = 다른거 읽고 있으니까 조금 이따 와

push한 다음에 문자열로 다 붙여

join은 배열에다 푸쉬한것을 붙여주는거
중간에 컴마있으면 안돼
화면이 안그려져
그래서 빈 무자열을 넣어야 해 파라미터에

컴마를 붙이고싶으면 컴마를 넣으면 돼